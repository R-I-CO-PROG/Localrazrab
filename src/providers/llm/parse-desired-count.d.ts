export declare function parseItemCountBounds(userPrompt: string): {
    min: number;
    max: number;
} | null;
export declare function parseDesiredItemCount(userPrompt: string): number | null;
export declare function defaultItemCount(userPrompt: string): number;
